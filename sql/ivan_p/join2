/*

pyrvoto uslovie ot zadachata:
select distinct sp.name from salesperson as sp, Customer as cm,
orders as od where od.cust_id=cm.id and 
od.salesperson_id=sp.id and cm.name+"Samsonic";
*/
/*
select sp.name, cm.name from salesperson as sp, Customer as cm,
orders as od where od.cust_id=cm.id and 
od.salesperson_id=sp.id
*/
/*
primer
create table not_jackson as select name, city from customer where city <> "jackson";
*/
4 uslovie ot zadachata:
create table highAchiever as select name, age from salesperson where salary >=100000;
